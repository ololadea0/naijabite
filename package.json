{
  "name": "naijabite",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install --include=dev --prefix frontend && npm install --include=dev --prefix backend && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
